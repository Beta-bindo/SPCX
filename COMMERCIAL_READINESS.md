# 商用发布门禁

本项目涉及实盘交易与授权控制。任何对外收费或交付前，必须逐项完成以下检查。

## 资金安全

- 实盘交易只能在「实盘双端」模式下开启，BA 与 Exness 必须同时真实连接。
- 禁止在一端模拟、一端实盘的混合状态下下单。
- 开仓必须确认两端真实持仓建立，不能把挂单接受当成成交。
- 平仓必须确认两端真实持仓消失，不能把平仓单接受当成完成。
- 任一开仓腿失败或成交状态不确定时，系统必须自动尝试回滚可能产生敞口的一腿。
- 自动回滚失败时，界面和日志必须明确提示用户立即检查单边敞口。
- Windows 小资金实盘验证必须覆盖：正常开仓、正常平仓、BA 失败、MT5 失败、网络超时、重复点击、自动下单触发。

## 授权与密钥

- 打包版必须启用授权门禁，不允许使用无授权构建。
- `server/.env` 不允许提交到代码仓库。
- 授权服务器必须设置 `TA_ADMIN_PASSWORD_HASH`，不使用默认或明文管理员密码。
- 授权服务器必须设置长度不低于 32 字符的 `TA_JWT_SECRET`。
- 授权服务器公网部署必须启用 HTTPS，并放在反向代理之后。
- 客户端 Windows 版本必须用 DPAPI 加密保存 API Secret 与 MT5 密码。

## 测试与发布

- Windows 环境必须安装依赖并跑完整测试套件。
- 每次发布必须重新跑聚焦资金安全测试：`python -m pytest tests/test_trading_safety.py -q`。
- GitHub Actions 或 Windows 打包机必须在打包前运行测试。
- 打包产物必须来自 `build.bat` 的授权构建。
- 发布包中不得包含 `.env`、本地数据库、测试账号、API Key、构建日志中的敏感信息。

## 合规与交付

- 用户协议必须说明软件不是投资建议，交易风险由用户自行承担。
- 隐私政策必须说明设备 ID、授权状态、交易统计上传范围。
- 对 Binance、Exness、MT5 的使用必须由用户自行确认符合当地监管与平台条款。
- 销售材料不得承诺收益率、稳赚、无风险套利。
- 必须建立故障响应流程：授权服务器故障、交易所 API 异常、用户单边敞口、版本回滚。
